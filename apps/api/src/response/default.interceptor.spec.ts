import { DefaultResponseInterceptor } from './default.interceptor';

describe('DefaultResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new DefaultResponseInterceptor()).toBeDefined();
  });
});
